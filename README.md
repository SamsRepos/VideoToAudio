# VideoToAudio

A simple Python desktop tool for converting video files into audio using FFmpeg.

## Requirements

- Python
- FFmpeg installed

## Installation

1. Set your FFmpeg path in 'config.json' (see 'example_config.json')

2. Install Python dependencies:

```bash
pip install -r requirements.txt
```

## How to Run

```bash
python vta.py
```
